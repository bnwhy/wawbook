import { BookProduct } from '../types/admin';
import { Theme } from '../types';

export const INITIAL_BOOKS: BookProduct[] = [];

export const wizardAssets = {
  avatars: {},
  hairstyles: {},
  beards: {},
};
